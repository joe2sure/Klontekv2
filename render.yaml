services:
  - type: web
    name: nextjs-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    env: node
    nodeVersion: 20.15.0  # Specify the Node version
    envVars:
      - key: NODE_ENV
        value: production